#include "main.c"
#include <stdlib.h>
/**
  * main - An Entry point funciton to print multiplication two argument.
  * @argc: The count of the arguments.
  * @argv: The vector of arguents count.
  * Return: Always return 0 or 1.
  */
int main(int argc, char *argv[])
{
	int num1;
	int num2;
	int result;

	if (argc != 3)
	{
		printf("Error\n");
	}
	num 1 = atoi(argv[1];
	num 2 = atoi(argv[2];
	result = num1 * num1;
	printf("%d\n", result);
	return (0);
}
