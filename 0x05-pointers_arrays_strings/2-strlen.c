#include "main.h"
#include <string.h>
/**
 * _strlen - is a function which founds the length of character
 *@s: is a pointer varible
 * Return: Always 0.
 */
int _strlen(char *s)
{
	return (strlen(s));
}
